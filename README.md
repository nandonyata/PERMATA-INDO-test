## Getting Started

First, go to backend directory and run the server:

```bash
cd backend
# and
npm i
# and
node app.js
    # or
nodemon app.js
```

then, go to frontend directory and run the client:

```bash
cd frontend
# and
npm i
# and
npm start
```
